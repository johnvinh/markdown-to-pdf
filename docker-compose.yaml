services:
  redis:
    image: 'redis:8.6.2'
    container_name: 'redis'
  app:
    build: .
    container_name: 'app'
    ports:
      - '8000:8000'
  worker:
    build: .
    container_name: 'worker'
    command: 'celery -A celery_app.app worker --loglevel=info'